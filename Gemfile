source "https://rubygems.org"

# Use standard Jekyll instead of the problematic github-pages gem
gem "jekyll", "~> 4.3"
gem "minima", "~> 2.5"

# Needed for Ruby 3.4+ environments to run Jekyll layout variables properly
gem "csv"
gem "base64"

# Crucial Windows execution fixes
gem "tzinfo-data", platforms: [:windows, :jruby]
gem "webrick", "~> 1.8"
